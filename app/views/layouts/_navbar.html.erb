<nav class="flex items-center justify-between px-6 py-4">
  <a href="/" class="flex items-center">
    <%= image_tag 'mia.png', alt: 'Mia Logo', class: 'h-8 w-auto mr-4' %>
    <h1 class="text-xl font-semibold text-slate-800"><%= t('.name') %></h1>
  </a>

  <nav class="space-x-6 text-slate-600">
    <% if user_signed_in? %>
      <%= link_to t('.chat_with_mia'), chat_index_path, class: 'hover:text-slate-800' %>
      <%= link_to t('.log_out'), destroy_user_session_path, data: { turbo_method: :delete }, class: 'font-medium hover:text-slate-800' %>
    <% else %>
      <%= link_to t('.log_in'), new_user_session_path, class: 'hover:text-slate-800' %>
      <%= link_to t('.sign_up'), new_user_registration_path, class: 'font-medium hover:text-slate-800' %>
    <% end %>
  </nav>
</nav>