<%# Usage:
  <%= render "components/button", text: "Start Chat", variant: "primary", size: "lg" %>

<%
  variant_class = "btn-#{variant || 'primary'}"
%>

<button class="btn <%= variant_class %> hover:animate-pulse">
  <%= text %>
</button>
